import { NewProjectContent } from "~/components/create-project/new-project-content";

export const metadata = {
  title: "New Project — MYJUNGLE",
};

export default function NewProjectPage() {
  return <NewProjectContent />;
}
