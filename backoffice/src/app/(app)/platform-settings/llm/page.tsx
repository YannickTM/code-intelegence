import { LLMProviderList } from "~/components/platform-settings/llm-provider-list";

export const metadata = {
  title: "LLM Provider Settings - MYJUNGLE Backoffice",
};

export default function PlatformLLMPage() {
  return <LLMProviderList />;
}
