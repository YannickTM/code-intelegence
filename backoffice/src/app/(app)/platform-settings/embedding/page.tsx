import { EmbeddingConfigForm } from "~/components/platform-settings/embedding-config-form";

export const metadata = {
  title: "Embedding Provider Settings - MYJUNGLE Backoffice",
};

export default function PlatformEmbeddingPage() {
  return <EmbeddingConfigForm />;
}
