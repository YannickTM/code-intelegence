import { ProfileForm } from "~/components/settings/profile-form";

export const metadata = {
  title: "Profile - MYJUNGLE Backoffice",
};

export default function ProfilePage() {
  return <ProfileForm />;
}
